<header class="header">
    <div class="container header-bg">
        {{> nav}}
        <div class="container">
            <div class="flex-grid">
              {{!-- TODO update image for course type --}}
                <section class="hero-col"><img class="hero-image"src="../public/assets/hero-image.png"/></section>
                <section class="hero-col">
                    <h3 class="h3-hero">{{course-title}}</h3>
                </section>
            </div>
        </div>
    </div>
</header>



{{> lesson }}

{{> quiz }}